window.fs = require('fs');
window.path = require('path');
window.child_process = require('child_process');
window.mongoose = require('mongoose');
window.logger = require('electron-log');
// console.log('DIR: ', __dirname);
// window.connectionSettings = require(path.join(__dirname, '/config'));
// window.Report = require('./models/reportmodel');
